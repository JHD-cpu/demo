spring:
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 127.0.0.1:8848
        file-extension: yml
        refresh-enabled: true
        extension-configs:
          - dataId: demo-service
        enable-remote-sync-config: true
springfox:
  documentation:
    enabled: false